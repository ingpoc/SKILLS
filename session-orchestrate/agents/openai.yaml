interface:
  display_name: "Session Orchestrate"
  short_description: "Advance product plans across bounded Codex tasks"
  default_prompt: "Use $session-orchestrate to resume or select the next substantial repository goal from current owner evidence, preserve authority pauses, and create exactly one eligible same-project successor when spawn_allowed is true without asking again."
