---
name: REPLACE-WITH-KEBAB-CASE-NAME
description: "REPLACE — one-paragraph description following project style. Lead with what the skill does (imperative). List explicit trigger phrases the operator might type — include adjacent verbs and proxy phrases (see references/description.md). State 'Use when …' explicitly. If the skill has multiple lanes, name each one with one-sentence selection criteria. If it auto-fires proactively on conditions (not just operator phrases), list those conditions. Spec cap is 1024 chars; local style permits longer when activation precision earns the verbosity — see references/description.md."
allowed-tools: Read, Write, Edit, Bash
---

# REPLACE-WITH-NAME — REPLACE-WITH-ONE-LINE-SUMMARY

> **Self-validate after edits.** Any change to this skill's files (SKILL.md, scripts/, references/, templates/, assets/) must be followed by `./scripts/validate.sh` from the skill directory. Hard findings → create-skill Optimize lane.

REPLACE — one short paragraph: what problem this skill exists to solve and what it produces. Anchor in operator intent ("the operator wants X"), not implementation ("this skill uses Y").

## Operating contract

| Field | Decision |
|---|---|
| Primary archetype | REPLACE — reference workflow / deterministic script workflow / browser UI / server API / artifact generator / agent orchestration / external service |
| Secondary archetypes | REPLACE — only list archetypes that change validation, scripts, or context loading |
| Operator trigger | REPLACE — concrete operator phrase or situation |
| Output | REPLACE — what this skill creates, changes, answers, or verifies |
| Success evidence | REPLACE — observable proof one run worked |
| Deterministic surface | REPLACE — scripts/checks that run without model judgment |
| Judgment surface | REPLACE — decisions that remain agent reasoning |
| Context loading | REPLACE — default context plus what is progressively disclosed |

## Entry — REPLACE (delete this section if single-flow)

If multi-lane, open with `AskUserQuestion`:

| Lane | When to choose |
|---|---|
| **LaneA** | REPLACE — concrete selection criteria |
| **LaneB** | REPLACE — concrete selection criteria |

Skip the question only when the typed prompt names a lane unambiguously:

| Typed prompt | Lane | Skip? |
|---|---|---|
| "REPLACE example" | LaneA | Yes |
| "REPLACE example" | LaneB | Yes |
| ambiguous variant | — | **No — ask.** |

## REPLACE — Main flow / Lane 1

### Preflight

1. REPLACE — preconditions that must hold before doing anything (existing file, clean git, named target, …). Refuse to proceed on missing precondition; ask via `AskUserQuestion` if operator can supply it.

### Do

```bash
# REPLACE — the actual steps, as runnable commands or a short procedure.
```

### Closeout

REPLACE — what must happen after the work (verification, audit, status update, memory write, etc.). Closeout is mandatory even on partial completion or crash.

#### Friction introspection

At closeout, classify any friction before changing this skill:

| Friction source | Action |
|---|---|
| Missing activation, workflow, preflight, closeout, validation, or context-boundary guidance | Update this skill. |
| Broken deterministic helper | Fix the script and add/adjust its test. |
| Target project/app/artifact bug | Fix the target, not this skill. |
| Environment, auth, provider, or rate-limit issue | Improve preflight/diagnostics only if the check is reusable. |
| One-off operator preference | Do not update the skill unless it becomes a repeated workflow need. |

## Hard rules

1. **REPLACE — invariant 1.** Why it matters.
2. **REPLACE — invariant 2.** Why it matters.
3. *(add as needed — keep numbered, terse, one rule per line)*

## Cross-references

- REPLACE — links to references/ files, scripts/ files, related skills, repo docs.
- [References](references/REPLACE.md) — REPLACE description
- [Scripts](scripts/REPLACE.py) — REPLACE description

## Why this skill exists

REPLACE — one paragraph. The specific problem this skill solves that nothing else does. Catches future-you wondering "do we still need this?" Don't write generic boilerplate — name the concrete failure mode this skill prevents.
